{/* <div className='text-white'>
      View Your Cart
      {cart.map((item, index) => (
        <div key={index}>
          {item.product_name} - {item.quantity} x {item.product_price} TK
        </div>
      ))}
    </div> */}